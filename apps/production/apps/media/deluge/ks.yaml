---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: deluge
  namespace: flux-system
spec:
  interval: 30m
  path: ./apps/production/apps/media/deluge
  prune: true
  wait: true
  timeout: 5m0s
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: media-shared
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  components:
    - ../../../../../components/volsync
    - ../../../../../components/alerts
  postBuild:
    substitute:
      APP: deluge
      VOLSYNC_CLAIM: deluge-config
