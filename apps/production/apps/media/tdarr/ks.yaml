---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tdarr
  namespace: flux-system
spec:
  interval: 30m
  path: ./apps/production/apps/media/tdarr
  prune: true
  wait: true
  timeout: 5m0s
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: media-shared
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  components:
    - ../../../../../components/volsync
    - ../../../../../components/alerts
  postBuild:
    substitute:
      APP: tdarr
      VOLSYNC_CLAIM: tdarr-config
