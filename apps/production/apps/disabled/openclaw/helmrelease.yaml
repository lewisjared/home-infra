---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openclaw
  namespace: openclaw
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    defaultPodOptions:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch

    controllers:
      main:
        strategy: Recreate
        initContainers:
          seed-config:
            image:
              repository: busybox
              tag: "1.37@sha256:1487d0af5f52b4ba31c7e465126ee2123fe3f2305d638e7827681e7cf6c83d5e"
            command:
              - sh
              - -c
              - |
                if [ ! -f /home/node/.openclaw/openclaw.json ]; then
                  cp /config-seed/openclaw.json /home/node/.openclaw/openclaw.json
                  echo "Seeded openclaw.json from ConfigMap (first boot)"
                else
                  echo "openclaw.json exists, skipping seed"
                fi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
        containers:
          main:
            image:
              repository: ghcr.io/openclaw/openclaw
              tag: "2026.4.7-1@sha256:8a32028cc8dfe693d7a080956b5602066eada278c23b97fc073fe29c3239b281"
            env:
              TZ: Australia/Melbourne
              # Bind to all interfaces (required for K8s service routing)
              OPENCLAW_GATEWAY_BIND: "lan"
              # Gateway port
              OPENCLAW_GATEWAY_PORT: &port 18789
              # State directory (persistent volume)
              OPENCLAW_STATE_DIR: /home/node/.openclaw
              # Node.js memory limit
              NODE_OPTIONS: "--max-old-space-size=2048"
              # Suppress browser launch in container
              BROWSER: "echo"
            envFrom:
              - secretRef:
                  name: openclaw-secret
              - configMapRef:
                  name: openclaw-env
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /healthz
                    port: *port
                  initialDelaySeconds: 15
                  periodSeconds: 10
                  timeoutSeconds: 3
                  failureThreshold: 3
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /readyz
                    port: *port
                  initialDelaySeconds: 15
                  periodSeconds: 10
                  timeoutSeconds: 3
                  failureThreshold: 3
              startup:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /healthz
                    port: *port
                  initialDelaySeconds: 10
                  periodSeconds: 5
                  timeoutSeconds: 3
                  failureThreshold: 30
            resources:
              requests:
                cpu: 100m
                memory: 1Gi
              limits:
                memory: 4Gi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL

    service:
      main:
        controller: main
        ports:
          http:
            port: *port

    persistence:
      state:
        enabled: true
        type: persistentVolumeClaim
        storageClass: rook-ceph-block
        accessMode: ReadWriteOnce
        size: 10Gi
        globalMounts:
          - path: /home/node/.openclaw
      config-seed:
        type: configMap
        name: openclaw-config
        advancedMounts:
          main:
            seed-config:
              - path: /config-seed
                readOnly: true
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
      cache:
        type: emptyDir
        globalMounts:
          - path: /home/node/.cache
