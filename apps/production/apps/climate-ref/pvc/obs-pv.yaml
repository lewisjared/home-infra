---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: climate-ref-obs-nfs
spec:
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: climate-ref-obs-nfs
  nfs:
    server: 10.10.20.20
    path: /mnt/tank/climate-ref/obs
  mountOptions:
    - nfsvers=4.2
    - hard
    - noatime
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: climate-ref-obs
  namespace: climate-ref
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: climate-ref-obs-nfs
  resources:
    requests:
      storage: 100Gi
